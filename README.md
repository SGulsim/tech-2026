# Документация проекта: Dating Bot

**Проект:** tech-2026  
**Автор:** Саралиева Гульсим

---

## 1. Общее описание

Цель проекта — разработка Telegram-бота для знакомств с системой анкет и ранжирования пользователей.

Система позволяет:

- регистрировать пользователей через Telegram
- создавать и редактировать анкеты
- загружать фотографии в S3
- просматривать анкеты других пользователей
- ставить лайки/пропуски
- формировать мэтчи
- ранжировать анкеты по трёхуровневому алгоритму
- использовать реферальную систему

---

## 2. Архитектура системы

Система построена по модульному принципу и состоит из следующих компонентов:

### 2.1 Компоненты

1. **Telegram Bot**
   - Интерфейс взаимодействия с пользователем
   - Обрабатывает команды (/start, /profile, просмотр анкет, лайки, пропуски)

2. **Backend API (FastAPI)**
   - Основная бизнес-логика
   - Обработка запросов от бота
   - Работа с базой данных

3. **PostgreSQL**
   - Основное хранилище данных
   - Содержит пользователей, анкеты, лайки, мэтчи, рейтинги

4. **Redis**
   - Кэширование пре-ранжированных списков анкет
   - Ускорение выдачи рекомендаций

5. **Celery**
   - Фоновые задачи
   - Пересчёт рейтинга пользователей

6. **RabbitMQ**
   - Очередь сообщений
   - Передача событий (лайки, пропуски, мэтчи)

7. **S3 (MinIO)**
   - Хранение изображений пользователей

---

## 3. Взаимодействие компонентов

### Основной сценарий:

1. Пользователь отправляет команду в Telegram
2. Бот отправляет запрос в Backend API
3. Backend:
   - обрабатывает логику
   - обращается к БД
   - при необходимости к Redis

4. При событиях (лайк, пропуск, мэтч):
   - событие отправляется в RabbitMQ
   - Celery обрабатывает событие
   - обновляет рейтинг

---

## 4. Схема базы данных

### 4.1 User

- id (PK)
- telegram_id
- referrer_id (FK)
- created_at

### 4.2 Profile

- id (PK)
- user_id (FK)
- name
- age
- gender
- city
- interests
- preferences
- bio
- photo_count
- completeness_score
- created_at

### 4.3 Profile_photo

- id (PK)
- profile_id (FK)
- s3_key
- url
- order_index

### 4.4 Like

- id (PK)
- from_user_id (FK)
- to_profile_id (FK)
- is_skip
- created_at

### 4.5 Match

- id (PK)
- created_at

### 4.6 Match_participant

- id (PK)
- match_id (FK)
- user_id (FK)
- initiated_chat

### 4.7 Rating

- id (PK)
- user_id (FK)
- profile_id (FK)
- level1_score
- level2_score
- final_score
- calculated_at

### 4.8 Referral

- id (PK)
- referrer_id (FK)
- referred_id (FK)
- created_at

---

## 5. Алгоритм ранжирования

### Уровень 1: Первичный рейтинг

- совпадение по возрасту, полу, городу
- заполненность анкеты
- количество загруженных фотографий
- соответствие предпочтениям пользователя

---

## 6. Кэширование (Redis)

- При начале сессии пользователю подгружается список анкет (например, 10)
- Анкеты сохраняются в Redis в пре-ранжированном виде
- При просмотре бот берёт анкеты из кэша
- При окончании списка — повторная загрузка из БД

---

## 7. Фоновые задачи (Celery)

Используются для:

- пересчёта рейтинга
- обработки событий из очереди RabbitMQ

---

## 8. Очереди сообщений (RabbitMQ)

Используются для:

- передачи событий лайков и пропусков
- передачи событий мэтчей
- асинхронной обработки обновления рейтингов

---

## 9. Хранение изображений

- Все изображения сохраняются в S3 (MinIO)
- В базе хранится только ссылка (s3_key, url)

---

## 10. Репозиторий

Проект размещён в GitHub-репозитории.

Структура проекта:

- bot/
- backend/
- db/
- docker/
- tests/

---

## 11. Технический стек

### Backend

- Python 3.11
- FastAPI — REST API
- SQLAlchemy — ORM

### База данных

- PostgreSQL — основное хранилище

### Кэширование

- Redis — кэш анкет и промежуточных данных

### Очереди и асинхронность

- RabbitMQ — брокер сообщений
- Celery — обработка фоновых задач

### Хранилище файлов

- MinIO (S3-совместимое хранилище)

### Инфраструктура

- Docker / Docker Compose
- GitHub Actions (CI/CD)

---

## 12. Схема базы данных (ER-диаграмма)

```
User (1) ─── (1) Profile
User (1) ─── (M) Like (from_user)
Profile (1) ─── (M) Like (to_profile)
User (1) ─── (M) Match (через Match_participant)
User (1) ─── (1) Rating
Profile (1) ─── (M) Profile_photo
User (1) ─── (M) Referral (referrer)
User (1) ─── (M) Referral (referred)

Profile:
- id, user_id, name, age, gender, city, interests, preferences, bio, photo_count, completeness_score

Like:
- id, from_user_id, to_profile_id, is_skip

Match:
- id, created_at

Match_participant:
- id, match_id, user_id, initiated_chat

Rating:
- id, user_id, profile_id, level1_score, level2_score, final_score, calculated_at

Referral:
- id, referrer_id, referred_id, created_at
```

---

## 13. Архитектура системы

```
        ┌───────────────┐
        │ Telegram User │
        └──────┬────────┘
               │
               ▼
        ┌───────────────┐
        │  Bot Service  │  — UI, команды, регистрация
        └──────┬────────┘
               │ HTTP
               ▼
    ┌──────────┼──────────┐
    ▼          ▼          ▼
┌─────────┐ ┌─────────────┐ ┌──────────────────┐
│ Profile │ │  Matching   │ │  Notification    │
│ Service │ │  Service    │ │  Service         │
│         │ │             │ │                  │
│ CRUD    │ │ ранжирование│ │ уведомления о    │
│ анкет,  │ │ лайки,      │ │ мэтчах,         │
│ фото    │ │ мэтчи       │ │ напоминания      │
└────┬────┘ └──────┬──────┘ └────────┬─────────┘
     │             │                 │
     └─────────────┼─────────────────┘
                   ▼
     ┌─────────────┼─────────────┐
     ▼             ▼             ▼
┌────────┐   ┌────────┐   ┌────────────┐
│Postgres│   │ Redis  │   │  RabbitMQ  │
└────────┘   └────────┘   └─────┬──────┘
     ▲             ▲             ▼
     │             │       ┌──────────┐
     └─────────────┼───────│ Celery   │
                   │       └──────────┘
          ┌────────┴──────┐
          │     MinIO     │
          │ (S3 storage)  │
          └───────────────┘
```

**Микросервисы:**

| Сервис                   | Назначение                                                       |
| ------------------------ | ---------------------------------------------------------------- |
| **Bot Service**          | Интерфейс Telegram, команды /start, /profile, показ анкет, лайки |
| **Profile Service**      | CRUD анкет, загрузка фото в S3                                   |
| **Matching Service**     | Алгоритм ранжирования, лайки, пропуски, мэтчи, кэш в Redis       |
| **Notification Service** | Уведомления о мэтчах, напоминания                                |

---

## 14. Схема дизайна системы (потоки данных)

### Сценарий: просмотр анкет

1. Пользователь запрашивает анкету
2. Backend проверяет Redis:
   - если есть — отдаёт из кэша
   - если нет — берёт из БД, ранжирует, сохраняет в Redis

3. Backend отправляет список анкет в Redis
4. Бот показывает анкету

---

### Сценарий: лайк

1. Пользователь ставит лайк
2. Backend:
   - записывает в БД
   - отправляет событие в RabbitMQ

3. Celery:
   - получает событие
   - пересчитывает рейтинг

---

### Сценарий: загрузка фото

1. Пользователь отправляет фото
2. Backend загружает файл в MinIO
3. Ссылка сохраняется в БД

---

## 15. Итог

На данном этапе:

- спроектирована архитектура системы
- описаны взаимодействия сервисов
- разработана схема БД
- выбраны технологии (PostgreSQL, Redis, RabbitMQ, Celery, S3)
- определены ключевые потоки данных

Архитектура ориентирована на масштабируемость, отказоустойчивость и разделение ответственности между компонентами.
